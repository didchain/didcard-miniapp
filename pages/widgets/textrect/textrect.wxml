<view class="vtext-rect">
  <view class="vlabel">
    <label>{{label}}</label>
  </view>
  <view wx:if="{{!content}}" class="vtext">
    <slot />
  </view>
  <view class="vtext" wx:else>
    <span> {{content}} </span>
  </view>
</view>
